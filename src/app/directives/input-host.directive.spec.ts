import { InputHostDirective } from './input-host.directive';

describe('InputHostDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHostDirective();
    expect(directive).toBeTruthy();
  });
});
